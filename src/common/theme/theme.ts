export default interface Theme {
  Main: {
    backgroundColor: string;
    textColor: string;
    secondaryTextColor: string;
  };
  Button: {
    backgroundColor: string;
    textColor: string;
    border: string;
    shadow: string;
  };
  Card: {
    backGroundColor: string;
    textColor: string;
    secondaryTextColor: string;
    border: string;
  };
}
